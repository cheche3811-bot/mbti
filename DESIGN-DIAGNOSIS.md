# MBTI 三维性格测试站 · 内容设计诊断与改进方案

> 诊断日期：2026-09-01
> 对象：https://cheche3811-bot.github.io/mbti/
> 方法：代码逐项核实 + 转化漏斗分析
> 出品：设计原型专家团（画统筹 / 许明需 / 彩格调 / 筑原型 / 严过审 / 交付达）

---

## 摘要

产品的**测评严谨性与视觉完成度都已达到很高水准**（48 题七级量表、正反向 5:5 平衡、21 条文献分级、55+16 个 SVG 头像、28 套分享文案），但存在 **1 个致命结构断点**与 **4 处传播闭环断裂**，导致：

- 投入最多的用户（答完 48 题）拿到的是**次级体验**
- 分享链路上，**图片和文案都无法把人带回站点**

这两类问题不解决，前面所有的严谨性与视觉投入都无法转化为增长。

---

## 一、P0 致命：48 题主路径断裂

### 事实

```bash
grep -cE "multi|三维|综合|screen-form" assets/js/app.js
# → 0
```

`app.js`（MBTI 答题与结果页逻辑）中，**通向三维综合分析的入口数为 0**。

### 用户实际经历

```
首页 → 选「做 MBTI 测试」→ 认真答 48 题（约 6 分钟）
  → 看到 MBTI 单结果页
  → 结束。没有任何入口通向三维报告。
```

想看三维报告，必须：回首页 → 选另一个入口 → **手动重新选择自己的类型**。

而手选类型会触发降级：

| 路径 | MBTI 向量精度 |
|---|---|
| 48 题作答 | 真实百分比（如外向性 74） |
| 手动选类型 | **固定 75% 估算值** |

代码依据（`share-engine.js` / `synthesis.js`）：
- `mbtiToVector()` — 使用真实 `percent` 数据
- `mbtiTypeToVector()` — `DEFAULT_STRENGTH = 75` 硬编码估算

### 判断

**这是全站最严重的问题。** 花 6 分钟的用户与花 30 秒的用户，最终看到的报告质量相同（甚至前者更差——因为他根本到不了三维页）。

产品把最好的内容（三维交叉 + 性格总结 + 成就徽章）放在了**投入最少的路径**上。

### 改进方向

1. MBTI 结果页增加醒目引导：**「+30 秒，解锁三维交叉报告」**
2. 传递已有的精确 `percent` 数据，不走估算降级
3. 三维表单预填 MBTI 结果，用户只需补生日 → 单字段成本
4. 文案强调增益而非重新开始：「你的 48 题结果已保留，再填生日即可」

---

## 二、P0 传播闭环 4 处断裂

分享是这个产品的核心增长引擎，但闭环上有 4 个断点，**每一个都足以让传播失效**。

### 2.1 结果无法回流

```bash
grep -cE "URLSearchParams|location.search|\?type=" assets/js/*.js
# → 全部 12 个文件均为 0
```

朋友点开你分享的链接，**只能看到首页**，看不到你的结果。

社交传播的核心动力是「看看 TA 是什么型」→ 现在这个动力直接断掉。

**改进**：结果页写入 URL 参数（如 `?t=INFJ-A&z=taurus&b=1990-05-15-14`），支持直接打开他人结果页 + 醒目的「测测我自己」按钮。

### 2.2 微信分享无缩略图

```bash
grep -E "og:image" index.html
# → 无匹配
```

`index.html` 有 `og:title` / `og:description`，但**没有 `og:image`**。链接发到微信只有标题文字，无视觉吸引力。

**改进**：制作一张品牌 OG 封面图（1200×630），或按类型动态生成。

### 2.3 CTA 指向虚无

`share-copy.json` 的 6 条 CTA：

```
你是哪一型？👉
来测测看你是什么 👉
好奇你们的结果 👉
评论区报个型？👉
三维交叉分析，比单看星座准 👉
48 题量表 + 星座 + 八字 👉
```

**全部以「👉」结尾，后面没有链接占位符。** 用户复制文案发出去，朋友看到一个指向虚无的箭头。

更严重的是：

```bash
grep -c "cta" assets/js/multi.js
# → 0
```

三维结果页的分享区**根本没有引用 CTA 列表**（仅 `share-engine.js` 内部使用）。

**改进**：CTA 模板加 `{url}` 变量，复制文案时自动拼接站点链接。

### 2.4 分享图无回链

```bash
grep -cE "github.io|qrcode|QR|url" assets/js/share-card-v2.js
# → 0
```

1080×2020 的分享长图设计精美，但**图上没有链接、没有二维码**。图片在微信里传播时，看到的人无法回到站点。

**改进**：分享图底部加二维码或短链文字（品牌区已预留空间）。

---

## 三、P1 情绪断层：学术诚实放错了位置

### 量化

| 区块 | 中文字数 |
|---|---|
| 性格总结骨架 | 96 |
| 方法与依据 | 184 |
| 免责声明 | **434** |
| **严谨性内容合计** | **618（骨架的 6.4 倍）** |

### 结果页区块顺序（问题所在）

```
1. 综合画像卡
2. 性格总结
3. 三个维度分别怎么说
4. 三维度详情卡
5. 五维特质对比
6. 交叉对比
7. 🔬 方法与依据（184 字）
8. ⚠️ 深色大免责声明（434 字）  ← 情绪冷水
9. 分享区（徽章 + 文案 + 生成图）  ← 分享按钮在这里
10. 底部操作
```

用户的实际心理曲线：

```
"我是稀有人格！只有 1.5%"        ← 情绪高点
        ↓
"MBTI 重测信度只有 50%"
"占星已被《自然》双盲实验否证"     ← 冷水浇头
"八字不属于现代科学范畴"
        ↓
"...要不要分享呢"                ← 情绪已散
```

### 判断

**严谨性本身是这个产品的稀缺优势，必须保留**——它是与市面上娱乐测试的核心差异点。问题不在内容，在**位置**。

### 改进方向

- 分享区**上移到性格总结之后**（第 3 位），趁情绪高点
- 严谨性内容改为**默认折叠**的「查看方法与依据」，保留完整内容但不阻断情绪
- 免责声明精简为一行摘要 + 展开详情，从「墙」变成「注脚」
- 关键：**不是删除严谨性，而是让它从"路障"变成"可查阅的背书"**

---

## 四、P1 答题激励缺失

```bash
grep -cE "milestone|阶段|halfway|加油" assets/js/app.js
# → 0
```

48 题纯线性推进，现有反馈仅：进度条、题号（`1 / 48`）、维度标签。

**48 题在移动端是不小的心理负担**，且首页明确标注"约 6 分钟"，第一屏就可能劝退一部分用户。

### 改进方向

| 手法 | 说明 |
|---|---|
| 分段里程碑 | 12/24/36 题处给反馈：「已完成 1/4，你的外向性倾向开始显现」 |
| 中途预览 | 24 题时展示已成型的维度倾向，制造"已有收获"感 |
| 进度可视化 | 现有进度条较弱，可强化为分段式（4 段 × 12 题） |
| 首页文案 | "约 6 分钟"改为"48 题 · 每题 5 秒"，降低感知负担 |

---

## 五、P2 徽章暴露过晚

```bash
grep -n "calcBadges" assets/js/multi.js
# → 仅在 buildShareSection()（第 285 行）内调用
```

9 条成就徽章（💎极稀有人格 / 🎯三观高度统一 / 🎭多面人格 / 🔥特质拉满…）**全部埋在第 9 位区块**。

徽章是最强的分享驱动力（「我是稀有人格」是天然的社交货币），却放在用户最可能看不到的位置。

**改进**：徽章前置到结果页顶部（画像卡下方），做成「解锁成就」的视觉效果。

---

## 六、P2 无社会证明

```bash
grep -cE "人测过|已有.*人|热门|排行" index.html
# → 0
```

首页没有任何社会证明。对陌生访客而言，缺少"别人都在玩"的信任信号。

**改进**（纯静态站可实现的方案）：
- 展示"16 型人口占比"数据（已有 CAPT 数据）→ 转化为「你可能是最稀有的那 1.5%」
- 本地累计测试次数（localStorage）
- 「热门类型」静态榜单

---

## 七、改进优先级

| 优先级 | 问题 | 预期影响 | 实现成本 |
|---|---|---|---|
| **P0** | 48 题 → 三维路径打通 | 极高 | 中 |
| **P0** | URL 参数回流 | 极高 | 中 |
| **P0** | CTA 拼接链接 | 高 | 低 |
| **P0** | 分享图加二维码/短链 | 高 | 低 |
| **P0** | og:image | 中 | 低 |
| **P1** | 分享区上移 + 严谨内容折叠 | 高 | 低 |
| **P1** | 答题里程碑激励 | 中 | 中 |
| **P2** | 徽章前置 | 中 | 低 |
| **P2** | 社会证明 | 中 | 低 |

**建议顺序**：先做 4 个低成本 P0（CTA / 二维码 / og:image / 分享区上移），当天可见效果；再做 2 个中成本 P0（路径打通 / URL 回流）。

---

## 八、设计层面待补（Phase 2 进行中）

CSS 令牌离散度扫描结果：

| 属性 | 不同取值种数 |
|---|---|
| `font-size` | **30 种** |
| `padding` | **30 种** |
| `gap` | **14 种** |

字号使用频次：12.5px（22 次）、13px（15 次）、13.5px（10 次）—— 三个肉眼无法区分的值，10.5～13.5px 共 7 个取值挤在 3px 区间内。

现有 `:root` 已有色彩/圆角/阴影/字体令牌，但**缺间距系统、字号阶梯、动效令牌**。

设计系统专家正在产出 `DESIGN-TOKENS.md`，含旧值→新令牌映射表，便于机械替换。

---

## 附：诊断方法说明

本报告所有结论均基于**代码实际核实**，非推测。每条问题均附可复现的 `grep` 命令与命中数。

诊断由需求发现分析师许明需执行，主理人画统筹逐条独立核实。其中「48 题路径断裂」为许明需发现并纠正了主理人的初始判断偏差（原判断认为主要矛盾是区块顺序）。
